import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        marginBottom: 10
    },
    inputGrid: {
        alignItems: 'center',
        flexDirection: 'row',
        justifyContent: 'center',
    },
    input: {
        width: '48%',
        marginHorizontal: 5,
    }
})